# This file contained an API key which was removed for security.
# Do not store API keys in code. Use environment variables instead.

print("Chat.py cleaned - API key removed for GitHub security.")
print("Use .env files or input() for keys in local development.")